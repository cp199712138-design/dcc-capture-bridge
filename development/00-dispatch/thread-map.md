# Instant Canvas Thread Map

Root project directory:

```text
<local Instant Canvas workspace>
```

Current working branch:

```text
codex/instant-canvas-scaffold
```

Threads:

```text
Instant Canvas 即时画布 / 00 项目总览
Instant Canvas 即时画布 / 01 UI
Instant Canvas 即时画布 / 02 Canvas
Instant Canvas 即时画布 / 03 API
Instant Canvas 即时画布 / 04 3ds Max
Instant Canvas 即时画布 / 05 Blender
Instant Canvas 即时画布 / 06 测试发布
```

The dispatcher owns this map. Child threads should not rename project threads.
